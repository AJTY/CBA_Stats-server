<%= form_for(@liga_hraci) do |f| %>
  <% if @liga_hraci.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@liga_hraci.errors.count, "error") %> prohibited this liga_hraci from being saved:</h2>

      <ul>
      <% @liga_hraci.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
