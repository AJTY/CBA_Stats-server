<h1>Listing liga_hracis</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @liga_hracis.each do |liga_hraci| %>
      <tr>
        <td><%= link_to 'Show', liga_hraci %></td>
        <td><%= link_to 'Edit', edit_liga_hraci_path(liga_hraci) %></td>
        <td><%= link_to 'Destroy', liga_hraci, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Liga hraci', new_liga_hraci_path %>
