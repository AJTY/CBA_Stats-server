<%= form_for(@liga) do |f| %>
  <% if @liga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@liga.errors.count, "error") %> prohibited this liga from being saved:</h2>

      <ul>
      <% @liga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
